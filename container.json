{
  "variables": {
    "timezone": "Asia/Tokyo",
    "image_from": "autechgemz/unbound-baseimage:latest",
    "unbound_root": "/chroot"
  },
  "builders":[
    {
      "type": "docker",
      "image": "{{user `image_from`}}",
      "run_command": ["-dit", "{{.Image}}", "/bin/bash"],
      "pull": false,
      "export_path": "container.tar"
    }
  ],
  "provisioners":[
    {
      "type": "ansible",
      "playbook_file": "container.yml",
      "user": "root",
      "ansible_env_vars": [
        "ANSIBLE_SSH_ARGS='-o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o AddKeysToAgent=no'"
      ],
      "extra_arguments": [
        "-e timezone={{user `timezone`}}",
        "-e unbound_root={{user `unbound_root`}}"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "apt-get -y purge python3 python3-apt",
        "apt-get -y autoclean",
        "apt-get -y autoremove",
        "rm -rf /var/lib/apt/lists/*",
        "rm -rf /var/cache/apt/*"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "docker-import",
      "repository": "autechgemz/unbound",
      "tag": "latest",
      "changes": [
        "ENV TZ {{user `timezone`}}",
        "ENV PATH {{user `unbound_root`}}/sbin:{{user `unbound_root`}}/bin:/usr/sbin:/sbin:/usr/bin:/bin",
        "ENV GOPATH {{user `unbound_root`}}",
        "ENV LD_LIBRARY_PATH {{user `unbound_root`}}/lib",
        "EXPOSE 53/tcp 53/udp",
        "ENTRYPOINT [ \"runsvdir\", \"-P\", \"\/services\/\" ]"
      ]
    }
  ]
}
